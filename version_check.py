import sys
import cv2
import numpy
import matplotlib
import ultralytics
import torch
#import keyboard

print(f"Python Version: {sys.version}")
print(f"OpenCV Version (cv2): {cv2.__version__}")
print(f"NumPy Version: {numpy.__version__}")
print(f"Matplotlib Version: {matplotlib.__version__}")
print(f"Ultralytics Version: {ultralytics.__version__}")
print(f"PyTorch Version (torch): {torch.__version__}")

# chcek keyboard version by using "pip show keyboard" in command prompt